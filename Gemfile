source 'https://rubygems.org'

# Jekyll
gem 'github-pages'
gem 'jekyll-github-metadata'
gem 'wdm', '>= 0.1.0' if Gem.win_platform?

# Travis CI
group :test do
  gem 'rake'
  gem 'html-proofer'
end